package com.example.springwebtask.Controller;

import com.example.springwebtask.Entity.LoginData;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

import java.util.ArrayList;
import java.util.List;

@RestController
public class ProductManagementSystemRestController {
//    List<LoginData> loginDataList = new ArrayList<>();
//    @PostMapping("/index")
//    LoginData loginCheck(@RequestBody LoginData loginData){
//        System.out.println("accessしました。");
////        if(loginData.getId().equals("id") && loginData.getPassWord().equals("password")) {
////            return "/menu";
////        }
//
//        //仮で受け取ったオブジェクトを返す。
//        return loginData;
//    }
}
